[package]
name = "memopaws-config"
version = "0.1.0"
edition = "2021"

[dependencies]
memopaws-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"

[dev-dependencies]
