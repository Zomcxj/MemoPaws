[package]
name = "memopaws-canvas"
version = "0.0.1"
edition = "2021"

[dependencies]
memopaws-core = { path = "../core" }
image = "0.25"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
